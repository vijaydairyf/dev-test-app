package com.devapp.devmain.dao;

import org.junit.Test;

/**
 * Created by x on 24/5/18.
 */
public class DaoTest {
    @Test
    public void findByFilterTest() throws Exception {
    /*    Dao dao = new CollectionRecordDao(null);
        Map<Key, Object> map = new HashMap();
        map.put(new Key("name", Dao.FilterType.EQUALS), "nishant");
        map.put(new Key("id", Dao.FilterType.NOT_EQUALS), "3");
        map.put(new Key("time", Dao.FilterType.GREATER_THAN), "4");
        map.put(new Key("date", Dao.FilterType.BETWEEN),new String[]{"5","6"});
        map.put(new Key("route", Dao.FilterType.IN), new String[]{"7","8","9"});
        String expected = "SELECT * FROM ReportsTable WHERE name = ? AND id != ? AND time > ? AND date BETWEEN ? AND ? " +
                "AND route IN (?,?,?)";
        Assert.assertEquals(expected, dao.findByFilterTest(map));*/
    }

}